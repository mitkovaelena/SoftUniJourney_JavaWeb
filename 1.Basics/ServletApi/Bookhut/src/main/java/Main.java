import entities.User;
import models.bindingModels.AddBookModel;
import models.bindingModels.LoginModel;
import models.viewModels.ViewBookModel;
import org.modelmapper.ModelMapper;
import repositories.BookRepositoryImpl;
import repositories.UserRepositoryImpl;
import services.BookService;
import services.BookServiceImpl;
import services.UserService;
import services.UserServiceImpl;
import utils.MappingUtil;

public class Main {
    public static void main(String[] args) {
//        UserService userService = new UserServiceImpl();
//        userService.createUser(new LoginModel("Eli", "Eli"));
//        System.out.println(userService.findByUsernameAndPassword("Eli", "Eli").getUsername());

    }
}
